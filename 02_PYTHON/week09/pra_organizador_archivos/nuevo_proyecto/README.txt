Dentro del directorio nuevo proyecto he creado los siguientes archivos, que serían la práctica:

- organizador_shutil.py y organizador_os.py:
	son los scripts de python definitivos uno hecho con el modulo suthil y el otro con el modulo os

- programa_ordenar_directorio.ipynb:
	es el jupyter notebook que he usado para hacer pruebas y luego pasar todo a los scripts .py

- carpetas organizador_os_exe y organizador_suthil_exe:
	son los dos directorios que contienen el ejecutable en windows .exe para que el script pueda ser ejecutado en
	cualquier máquina que no tenga python y que el usuario pueda elegir su ruta propia, con los nombres de carpeta
	propios que tenga su ordenador y organizar los directorios

- carpeta descargas:
	es la carpeta que yo me he creado para hacer pruebas y comprobar que todo funciona

- carpeta iconos:
	contiene los iconos que he usado para crear el icono de escritorio del ejecutable .exe